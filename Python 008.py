import math #importa a biblioteca completa de math

num = int(input('Digite um número: '))

raiz = math.sqrt(num)


from math import sqrt #importa somente o comando sqrt da biblioteca math

num = int(input('Digite um número: '))

raiz = sqrt(num) #não precisa usar o math antes da função sqrt